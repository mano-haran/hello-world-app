name: Demo workflow

on:
  - push
  - workflow_dispatch

jobs:
  display-vars:
    runs-on: ubuntu-latest
    steps:
      - name: print vars
        run: "echo ${{ toJSON(vars) }}"
      - name: print env
        run: "echo ${{ toJSON(env) }}"
